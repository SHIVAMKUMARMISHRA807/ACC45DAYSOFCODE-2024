#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--)
	{
	    int a,b,f,s;
	    cin>>a>>b;
	    f=(a*10);
	    s=(b*5);
	    if(f>s)
	    {
	        cout<<"FIRST"<<endl;
	    }
	    else if(f<s)
	    {
	        cout<<"SECOND"<<endl;
	    }
	    else 
	    {
	        cout<<"ANY"<<endl;
	    }
	}
	return 0;
}
