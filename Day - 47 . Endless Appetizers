#include <iostream>
using namespace std;

int main() {
	int t,x,y,r,xsticks,tsticks;
	cin>>t;
	while(t--)
	{
	    cin>>x>>y>>r;
	    xsticks=r/30;
	    tsticks=x+xsticks;
	    if(tsticks%y==0)
	        cout<<tsticks/y<<endl;
	    else
	        cout<<(tsticks/y)+1<<endl;
	}
	return 0;
}
